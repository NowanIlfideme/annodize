name: annodize
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  # might be faster
  - pandas=1.4
  - pyspark=3.2
  # for examples?
  - jupyter
  - pip:
      - "-e .[dev,pandas,pyspark,docs]"
      - beartype
      - typeguard
      - runtype
